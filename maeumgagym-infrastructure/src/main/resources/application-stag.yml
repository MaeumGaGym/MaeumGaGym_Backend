spring:
    config:
        activate:
            on-profile: stag

    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                show_sql: true

management:
    endpoint:
        health:
            show-details: always

server:
    domain:
        front-domain: ${FRONT_LOCAL}
        back-domain: ${BACK_DOMAIN}
    servlet:
        context-path: /maeumgagym
